

const Weather = (props) => {

    if (props.city)
        return (
        
            <div> 
                <h1> Weather in {props.city} </h1>
            </div>
        )
                    
                    

 
}
export default Weather